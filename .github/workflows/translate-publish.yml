name: Legacy language workflow

on:
  workflow_dispatch:

# The canonical workflow is build-and-publish-langpacks.yml.
# This file intentionally does nothing to prevent duplicate releases.
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use build-and-publish-langpacks.yml"
